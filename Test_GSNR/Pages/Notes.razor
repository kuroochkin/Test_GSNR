@page "/notes"

<div class="container-fluid">
    <div class="row">
        <div class="col">
            <h2>Заметки</h2>

            <table class="table table-striped">
                <thead class="table-dark">
                    <tr>
                        <th scope="col">Название</th>
                        <th scope="col">Описание</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                @foreach(var item in notes)
                {
                    <tbody>
                        <tr>
                            <td>
                                @item.Title
                            </td>
                            <td>
                                @item.Description
                            </td>
                            <td>
                                
                            </td>
                        </tr>
                    </tbody>
                }
            </table>

            <div class="row">
                <div class="col">
                    <input class="form-control" placeholder="Введите название заметки" @bind="newNote"/>
                </div>
                <div class="col">
                    <button class="btn btn-success" @onclick="AddNote">Добавить</button>
                </div>
            </div>
            
        </div>
    </div>
</div>

@code {
    private IList<NoteItem> notes = new List<NoteItem>();

    private string newNote;

    private void AddNote()
    {
        if(!string.IsNullOrWhiteSpace(newNote))
        {
            notes.Add(new NoteItem { Title = newNote });
            newNote = string.Empty;
        }
    }
}
